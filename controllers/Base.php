<?php

namespace Rhymix\Modules\IndexNow\Controllers;

use ModuleObject;

/**
 * 컨트롤러 클래스에서 공통으로 사용할 만한 기능은 여기에 넣으면 된다.
 * ModuleObject를 상속받으므로 라이믹스 컨트롤러의 모든 기능을 지원한다.
 *
 * 트리거 등 흔히 코드를 지저분하게 만들 만한 것들은 module.xml에서 정의하였으므로
 * 간단한 모듈이라면 아무 것도 없는 것이 정상이다.
 */
class Base extends ModuleObject
{

}
